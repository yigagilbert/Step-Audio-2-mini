# Core project requirements.
-r requirements.txt

# Evaluation extras.
scipy>=1.11.0
unbabel-comet>=2.2.0
onnxruntime-gpu>=1.18.0

# Cascade TTS baseline with Sunbird/orpheus-3b-tts-multilingual. The vLLM pin
# keeps the install aligned with the torch/torchaudio 2.5.x pair in the setup
# script.
vllm==0.7.3
snac>=1.2.1

# BLASER 2.0 via SONAR is intentionally not installed by default. fairseq2
# wheels must exactly match the installed PyTorch/CUDA build; the Torch 2.5.x
# environment used for Step-Audio/Orpheus does not have a current matching
# fairseq2 CUDA 12.4 wheel. Use --skip-blaser here, or run BLASER separately.
